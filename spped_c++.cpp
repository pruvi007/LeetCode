
// the following lines increased 64 ms to 4ms (remember to paste these lines above your code)
static int speed_increase=[]() { 
    std::ios::sync_with_stdio(false);
    cin.tie(NULL);
    return 0; 
} ();